# user-center (paused 2026-05-09)

This was started as the end-user-facing Next.js app at `user.develop.cc`
to replace the FastAPI 380px login card. **Paused** — the operator
asked us to focus on the OPERATOR-facing admin tools first, and put
everything in `ssl-service`'s admin SPA (one-stop ops console).

The end-user user-center (login / register / dashboard) is still on
the roadmap and will be built later — at that point this folder
becomes the actual app.
