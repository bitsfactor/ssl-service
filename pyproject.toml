[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ssl-service"
version = "0.1.0"
description = "Docker-based SSL service controller backed by PostgreSQL"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=42.0.0,<47.0.0",
  "psycopg[binary]>=3.1.18,<4.0.0",
  "psycopg_pool>=3.2.0,<4.0.0",
  "PyYAML>=6.0.1,<7.0.0",
  "paramiko>=3.4.0,<4.0.0",
]

[project.optional-dependencies]
test = [
  "pytest>=8.2.0,<9.0.0",
  "pexpect>=4.9.0,<5.0.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Bundle the static admin SPA and the New Service template tree.
# templates/service/ uses a `_dot_` filename prefix instead of literal
# dotfiles so source distributions reliably include every file (some
# packaging paths drop hidden files); the renderer strips `_dot_` →
# `.` at write time.
"ssl_proxy_controller" = ["static/*", "static/**/*", "templates/*", "templates/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
